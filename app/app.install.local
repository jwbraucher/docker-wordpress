# downstream project installation example
sudo -u "${app_user}" /bin/bash -ex << END_OF_SUDO
cd "${app_home}"

# Local wordpress install
wp theme install zerif-lite
wp theme install responsiveboat --activate

END_OF_SUDO
