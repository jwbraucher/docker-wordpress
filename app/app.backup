#!/bin/bash -ex
# vim: set filetype=sh :

# This script backs up the application to an archive file

echo Backing up Wordpress
sudo -u "${app_user}" /bin/bash -x << END_OF_SUDO
cd "${app_home}"

wp backupwordpress backup
find wp-content/ -name backup.zip \
    -exec mv {} ${backup} \;

END_OF_SUDO
